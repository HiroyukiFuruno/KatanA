# lefthook.yml
output: false

pre-commit:
  parallel: false
  commands:
    formatting:
      glob: "*.rs"
      run: make fmt
    clippy:
      glob: "*.rs"
      run: make lint
    ast-lint:
      glob: "*.rs"
      run: make ast-lint

pre-push:
  commands:
    check-strict:
      run: make -j 2 check
      glob: "*.{rs,toml,lock}"
      only:
        - ref: master
        - ref: release/*

    check-light:
      run: make check-light
      glob: "*.{rs,toml,lock}"
      skip:
        - ref: master
        - ref: release/*
