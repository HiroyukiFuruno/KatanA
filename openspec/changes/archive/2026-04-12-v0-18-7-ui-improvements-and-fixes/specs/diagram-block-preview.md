## MODIFIED Requirements

### Requirement: ダイアグラム全画面表示の集中度向上

ダイアグラムを全画面表示する際のオーバーレイ背景は、不透明（透過なし）でなければならない（SHALL）。

#### Scenario: 不透明な全画面オーバーレイ

- **WHEN** ユーザーがダイアグラムの「全画面表示」ボタンをクリックした時
- **THEN** 背後のエディタやプレビューが完全に見えない不透明な背景のモーダルが表示される
- **THEN** これにより、ユーザーはダイアグラムの内容に完全に集中できる
