## ADDED Requirements

### Requirement: tab-group-explorer-integration
エクスプローラーのコンテキストメニューから、直接タブグループを操作（作成・追加）できなければならない（SHALL）。

#### Scenario: ディレクトリからのタブグループ作成
- **WHEN** ユーザーがエクスプローラーでディレクトリを右クリックし、「ディレクトリからタブグループを作成」を選択した時
- **THEN** そのディレクトリ配下の全ファイル（再帰的またはフラット）を含む新しいタブグループが作成され、開かれる
